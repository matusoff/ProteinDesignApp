# Avoid Gradio 5.0.x (stale gradio_client vs Pydantic 2.11+ JSON Schema). HF pins gradio[oauth,mcp]; do not cap pydantic — mcp extra needs pydantic>=2.11.
gradio>=5.28.0,<6
# HF Spaces may preinstall huggingface_hub 1.x; Gradio 5.x oauth still imports HfFolder (removed in hub 1.0)
huggingface_hub>=0.25.0,<1.0
biopython>=1.84
pandas>=2.2.0
matplotlib>=3.8.0
numpy>=1.26.0
# Pandas 3.x checks jinja2 at import time
jinja2>=3.1.5
# Python 3.13 removed stdlib audioop; Gradio/pydub need this on 3.13+
audioop-lts>=0.2.0; python_version >= "3.13"
